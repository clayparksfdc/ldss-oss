---
title: "Color"
description: "These styling hooks contain the complete set of values based on their semantics, brand, neutral, and feedback colors. Only use in edge cases where a semantic UI color does not make sense."
category: "foundation"
slug: "color--system-palette"
status: "published"
lastModified: "2026-03-11"
---

## System Palette

These styling hooks contain the complete set of values based on their semantics, brand, neutral, and feedback colors. Only use in edge cases where a semantic UI color does not make sense.

- `--slds-g-color-neutral-base`is used for various UI elements such as text, surfaces, and non-functional UI.
- `--slds-g-color-brand-base` (electric-blue) is used for buttons, hover states, and selected or active states. Do not use for decorative purpose.
- `--slds-g-color-error-base` (pink), `--slds-g-color-warning-base` (yellow), `--slds-g-color-success-base` (teal), and `--slds-g-color-blue` is only to be used for those feedback or alert states.


`--slds-g-color-neutral-base`is used for various UI elements such as text, surfaces, and non-functional UI.

`--slds-g-color-brand-base` (electric-blue) is used for buttons, hover states, and selected or active states. Do not use for decorative purpose.

`--slds-g-color-error-base` (pink), `--slds-g-color-warning-base` (yellow), `--slds-g-color-success-base` (teal), and `--slds-g-color-blue` is only to be used for those feedback or alert states.

### Neutral Colors

### Brand Colors

### Error Colors

### Warning Colors

### Success Colors
