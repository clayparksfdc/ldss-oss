---
title: "Grid"
slug: "grid--examples"
category: "develop"
parent: "grid"
order: 0
---

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-default-left-to-right
title: Utilities With Code Grid - Grid Default Left To Right
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-intro-4-col-with-gutters
title: Utilities With Code Grid - Grid Intro 4 Col With Gutters
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-intro-12-col
title: Utilities With Code Grid - Grid Intro 12 Col
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-column-width-2-col
title: Utilities With Code Grid - Grid Column Width 2 Col
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-column-width-all-options
title: Utilities With Code Grid - Grid Column Width All Options
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-column-width-all-fraction-options
title: Utilities With Code Grid - Grid Column Width All Fraction Options
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-column-gutters-2-col-utilities
title: Utilities With Code Grid - Grid Column Gutters 2 Col Utilities
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-column-gutters-2-col-utilities
title: Utilities With Code Grid - Grid Column Gutters 2 Col Utilities
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-column-gutters-2-col-pull-padded
title: Utilities With Code Grid - Grid Column Gutters 2 Col Pull Padded
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-direct-column-gutters
title: Utilities With Code Grid - Grid Direct Column Gutters
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-all-children-column-gutters
title: Utilities With Code Grid - Grid All Children Column Gutters
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-column-wrapping-3-col
title: Utilities With Code Grid - Grid Column Wrapping 3 Col
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-column-reordering-3-col
title: Utilities With Code Grid - Grid Column Reordering 3 Col
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-column-nesting
title: Utilities With Code Grid - Grid Column Nesting
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-default-left-to-right
title: Utilities With Code Grid - Grid Default Left To Right
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-vertical-align
title: Utilities With Code Grid - Grid Vertical Align
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-horizontal-reversed
title: Utilities With Code Grid - Grid Horizontal Reversed
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-vertical-aligned-reversed
title: Utilities With Code Grid - Grid Vertical Aligned Reversed
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-responsive-2-col-mobile-desktop
title: Utilities With Code Grid - Grid Responsive 2 Col Mobile Desktop
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-responsive-2-col-mobile-tablet-desktop
title: Utilities With Code Grid - Grid Responsive 2 Col Mobile Tablet Desktop
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-responsive-reordering-2-col-mobile-desktop
title: Utilities With Code Grid - Grid Responsive Reordering 2 Col Mobile Desktop
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-horizontal-align-center-basic
title: Utilities With Code Grid - Grid Horizontal Align Center Basic
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-horizontal-align-center-variable
title: Utilities With Code Grid - Grid Horizontal Align Center Variable
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-horizontal-align-spaced
title: Utilities With Code Grid - Grid Horizontal Align Spaced
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-horizontal-align-spread
title: Utilities With Code Grid - Grid Horizontal Align Spread
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-horizontal-align-end
title: Utilities With Code Grid - Grid Horizontal Align End
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-content-alignment-bump-left
title: Utilities With Code Grid - Grid Content Alignment Bump Left
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-content-alignment-bump-right
title: Utilities With Code Grid - Grid Content Alignment Bump Right
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-vertical-align-start
title: Utilities With Code Grid - Grid Vertical Align Start
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-vertical-align-center
title: Utilities With Code Grid - Grid Vertical Align Center
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-vertical-align-end
title: Utilities With Code Grid - Grid Vertical Align End
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-vertical-align-absolute-center
title: Utilities With Code Grid - Grid Vertical Align Absolute Center
height: 400px
```

```storybook
url: https://slds2-metadata-stories-54d63ecb6c29.herokuapp.com/iframe.html?id=utilities-with-code-grid--grid-vertical-align-individual
title: Utilities With Code Grid - Grid Vertical Align Individual
height: 400px
```

Grid utility classes help create responsive layouts. These classes use the CSS flexbox model to align and distribute elements to ensure a consistent and scalable layout structure across Salesforce applications.

## Examples

### Basic Grid

Our most commonly used grid has standard 16px gutters. If you don’t have a design that calls for something more specific, this is the grid example you’ll want to use.

1. Add a grid container by adding `slds-grid` to an HTML element
2. Add as many `slds-col` elements as you want inside of your grid container

Initially, each column takes an equal percentage of the width available. In this case, that's 33.3% of the container’s width. Once content is added, the sections expand as needed to fit the content if sizing classes are not used.

Adding a fourth column means they'll each take up 25% of the width of the container.

### 12 Column Support

You can have up to 12 columns in your grid.

### Column Widths

If you don’t want the columns to have widths based on their content, you can easily control the width of the columns by adding column width classes. The following example sets the width of the first column to 66% (or 2 of 3) of your `slds-grid` container and the second column to 33% (or 1 of 3) of your `slds-grid` container.

ℹ️** Tip**

SLDS size classes set widths in percentages. This keeps your layout fluid to the width of your `slds-grid` container.

Here's an example of all the available width classes for a 12-column grid:

Width class names are written to support multiple syntaxes for fraction formats. You can use either `slds-size_1-of-2` or `slds-size_6-of-12`, both styles result in the column taking up 50% of the width.

### Column Gutters

By default, grid columns don't have gutters. This allows flexibility to add and customize gutter widths to your liking for each column or grid container.

You can easily add spacing to your grid by applying `slds-gutters` to the `slds-grid` container. This will add gutters to your columns and align the edges of your grid. In the following example, adds `slds-gutters` to give you a default gutter size of 24px, with 12px on each side.

ℹ️ **Note**

The default `slds-gutters` is subject to change over time, but it's a great way to make sure that when we upgrade, you upgrade.

#### Modifying with Gutter Classes

You can modify the width of the gutters between columns by using these classes.


| **Class** | **Width** |
| --- | --- |
| `slds-gutters_x-small` | 16px |
| `slds-gutters_small` | 24px |
| `slds-gutters_medium` | 32px |
| `slds-gutters_large` | 48px |
| `slds-gutters_x-large` | 64px |

#### Modifying Gutters with Padding

You can also add different gutter widths to your columns by adding a padding utility (eg.`slds-*p*-[*location]*_[*size])*`to the `slds-col` element. The `*[location]*` can be `left`, `right`, `top`, `bottom`, `vertical`, `horizontal`**,** or `around`. The `*[size]*` can be `xxx-small`, `x-small`, `small`, `medium`, `large`, `x-large`, `xx-large`. For example, use `slds-p-left_xxx-small` for a 2px gutter on the left.


| **Class** | **Definition** |
| --- | --- |
| `slds-p-left-*[size]*` | Add padding to the left |
| `slds-p-right-*[size]*` | Add padding to the right |
| `slds-p-top-*[size]*` | Add padding to the top |
| `slds-p-bottom-*[size]*` | Add padding to the bottom |
| `slds-p-vertical-*[size]*` | Add padding to top and bottom of each row |
| `slds-p-horizontal-*[size]*` | Add padding to left and right of each column |
| `slds-p-around-*[size]*` | Add padding to top, bottom, left, and right of a cell |
| `slds-p-*[location]*_xxx-small` | 2px width |
| `slds-p-*[location]*-small` | 4px width |
| `slds-p-*[location]*_x-small` | 8px width |
| `slds-p-*[location]*_small` | 12px width |
| `slds-p-*[location]*_medium` | 16px width |
| `slds-p-*[location]*_large` | 24px width |
| `slds-p-*[location]*_x-large` | 32px width |
| `slds-p-*[location]*_xx-large` | 48px width |

This example adds gutters of 16px on each side (a total of 32px) in between each column.

Adding 16px of space on both sides of every column adds 16px of padding on the left and right of your container. To remove this unwanted space, add a `slds-grid_pull-padded-[*size]*` class to the `slds-grid` container. For the `*[size]*` portion of this class, use the same spacing utility size you've applied to the outermost columns.

#### Direct Columns Only

By default, `slds-gutters` applies to *any* column nested within the grid. If you only want **direct child columns** to receive gutters, use the `slds-gutters_direct` class.

To modify the width of the gutters between the columns, use these classes.


| **Class** | **Width** |
| --- | --- |
| `slds-gutters_direct-x-small` | 16px |
| `slds-gutters_direct-small` | 24px |
| `slds-gutters_direct-medium` | 32px |
| `slds-gutters_direct-large` | 48px |
| `slds-gutters_direct-x-large` | 64px |

Next is the same example without using the `slds-gutters_direct` class:

### Column Wrapping

Columns align horizontally unless you tell them to do something else. If you're using `size` classes (for example `slds-size_4-of-12`) and the total number of columns in a row exceeds 12, you can tell the grid container to wrap your columns to a new row. Just add `slds-wrap` to the `slds-grid` container.

### Column Reordering

ℹ️ **Accessibility Concern**

If you change the order of columns without changing their position in the markup, your page can be hard to read for people using technology like screen readers. To stay WCAG compliant, make sure the visual order of columns matches the order in the markup.

### Column Nesting

Nested columns give you more flexibility in your designs. To create nested columns, add another `slds-grid` container inside your column. Then, add as many `slds-col` elements inside that container as you want.

### Grid Flow

With the `slds-grid` class, you can control the flow of your grid. By default, a grid flows from left to right on the horizontal axis. Changing it to flow from top to bottom on the vertical axis is as easy as adding `slds-grid_vertical` to the `slds-grid` container.

#### Left to Right

The default flow is left to right on the horizontal axis.

#### Top to Bottom

To switch the grid to flow top to bottom on the vertical axis, add `slds-grid_vertical` to the `slds-grid` container.

#### Reversed

Some situations call for reversing the visual flow of your columns. You can do this both horizontally and vertically.

**Right to Left**

To reverse the horizontal flow, add the class `slds-grid_reverse` to the `slds-grid` container.

**Bottom to Top**

To reverse the vertical flow, add the class `slds-grid_vertical-reverse` to the `slds-grid` container.

### Creating Responsive Layouts

SLDS sizing and ordering utilities use a mobile-first approach, and support responsive breakpoints that change the layout at certain viewport sizes. The SLDS grid system becomes responsive to the viewport size when you specify responsive sizing and ordering utility classes. These classes include a breakpoint name in the SLDS `size` and `order` classes, for example `slds-medium-size` and `slds-medium-order`.

Add responsive sizing utility classes to an element, making sure they're in the correct order. Each class overrides the previous breakpoint, so list them from smallest to largest viewport.

The first sizing utility is applied at the smallest viewport width and continues to be applied until a responsive sizing utility for a specific size breakpoint overrides it.

In some rare cases, you may need a sizing utility that works up to a specified breakpoint. In those cases, use the classes that begin with `max-` to achieve your goal.

#### Responsive Layout Options

This overview table outlines the default behavior for columns and shows how that behavior can change for responsive breakpoints.


|  | **Default** | **Small (>= 480px)** | **Medium (>=768px)** | **Large (>=1024px)** |
| --- | --- | --- | --- | --- |
| **Width class prefix** | `slds-size` | `slds-small-size` | `slds-medium-size` | `slds-large-size` |
| **# of columns** | 1, 2, 3, 4, 5, 6, 7, 8 and 12 |  |  |  |
| **Column ordering prefix** | `slds-order` | `slds-small-order` | `slds-medium-order` | `slds-large-order` |
| **Max # of reorders** | 12 |  |  |  |

**Small (>= 480px)**

**Medium (>=768px)**

**Large (>=1024px)**

**ℹ️ **To make sure columns wrap as they exceed a total column count of 12, add `slds-wrap` to the `slds-grid` container in responsive layouts.

**Mobile/Desktop Example**

Let's set a layout for mobile viewports and override it for desktop directly in our HTML. First set the columns to default widths for mobile. Then change the layout when the screen is the size of a desktop. The following example sets up a stacked layout for mobile/tablet devices. When the viewport is bigger than 1024px, the layout changes to a nice two-column layout with the main content area taking up 8 columns and the second content area taking up 4 columns.

**Mobile/Tablet/Desktop Example**

If your designs need a bit more flexibility, you can accomplish that by establishing different layouts for mobile, tablet, and desktop. In the following example, the default layout is stacked. If the browser's viewport width is more than 768px, the layout changes so that each column takes up 50% or 6 columns of the `slds-grid` container. If the viewport width exceeds 1024px, the layout is redrawn to 66% or 8 columns for the primary content area and 33% or 4 columns for the secondary content area.

**Conditional Column Reordering**

With flexbox, you can rearrange your columns visually. The SLDS grid system makes it easy to reorder the columns visually in a responsive way. To do this, use the same naming convention as sizing. Insert the breakpoint name before `order` in `slds-order`, and then add an integer to indicate where you want the column to appear visually. The column with the lowest value of `slds-order` class appears first in the column order. If you don't define an order, the standard order is applied. This means that a column without an order class renders before `slds-order_1`. The following example sets the first column to be displayed last in the order of 3. Then when the viewport exceeds a width of 1024px, it changes the visual order of the first column back to 1.

### Alignment

Because the grids are built on flexbox, they enable you to do some interesting things with alignment on both a horizontal axis and a vertical axis. You can add an alignment utility to the `slds-grid` container.

#### Horizontal Axis

**Content centered**

If you want your columns to grow from the center of the horizontal axis, apply the class `slds-grid_align-center`.

**Setting variable widths based on content**

Another powerful technique is letting a column shrink to the width of the content inside of it, while explicitly setting widths for other columns. This is achieved by using the `slds-shrink-none` or `slds-grow-none` utility classes or simply removing the `slds-col` class from the column element.

**Content evenly spaced out**

To evenly distribute columns on the horizontal axis with an equal amount of white space separating the columns, apply the class `slds-grid_align-space`.

**Content evenly spread out from edges**

To spread out your columns on the horizontal axis, with the first column starting at the start of your main axis and last item ending at the far end of your main axis, apply the class `slds-grid_align-spread`.

**Content right-justified**

If you want your columns to grow from the end of the horizontal axis, apply the class `slds-grid_align-end`.

**Content alignment with bump**

To "bump" a single or multiple grid items, apply the class `slds-col_bump-{direction}`, with `{direction}` being either left, right, top or bottom, to a grid item.

**Bump Left**

In the example below, applying the left bump class to item 5 bumps items 1 through 4 to the left.

**Bump Right**

In the example below, applying the right bump class to item 5 bumps item 6 to the right.

#### Vertical Axis

ℹ️ **Note**

To vertically align elements on a cross-axis of a `slds-grid` container, the elements need available vertical white space. You can usually achieve this by applying a height to the `slds-grid` container.

**Content top**

To align a single row or multi-line rows to the beginning of the cross axis, apply the class `slds-grid_vertical-align-start`.

**Content centered**

To vertically center align a single row or multi-line rows to the height of a `slds-grid` container, apply the class `slds-grid_vertical-align-center`. When you use `slds-grid_vertical-align-center`  with `slds-grid_align-center`, the effect is to horizontally and vertically center align your content in the center of the `slds-grid` container.

**Content bottom**

To align a single row or multi-line rows to the end of the cross axis, apply the class `slds-grid_vertical-align-end`.

**Content absolutely centered**

**Individual alignment**

To specify the vertical placement of grid items on the cross axis, you can apply `slds-align-top`, `slds-align-middle`, and `slds-align-bottom` to an `slds-col` element.

### Page Containers

Optionally, you can contain your page content by adding the class `slds-container_*width*` to a top-level element in your page structure.

Containers are available in four widths: small, medium, large, and x-large. They are rendered using `max-width`to use the maximum width available to display their content. This means the content expands to fill the space until it reaches the width limit set for the container by `slds-container_*`.


| **Class** | **Width** |
| --- | --- |
| `slds-container_small` | 480px |
| `slds-container_medium` | 768px |
| `slds-container_large` | 1024px |
| `slds-container_x-large` | 1280px |

Additionally, you can center the content of your page by adding `slds-container_center`.
